@model System.Data.DataSet
@{
    ViewBag.Title = "NodoAluno";
}
<div class="content">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            Nodos
        </h1>        
    </section>

    <!-- Main content -->
    <section class="content">

        <!-- Modal -->
        <div class="row">
            <div class="col-xs-12">
                <div class="box">
                    <!-- /.box-header -->
                    <div class="box-body">
                        <table id="example2" class="table table-bordered table-hover">
                            
                            <thead>
                                <tr>
                                    <th>Nome</th>
                                    <th>Resolver</th>
                                </tr>
                            </thead>
                            <tbody> 
                                @if (Model.Tables[0].Rows.Count > 0)
                                {
                                    foreach (System.Data.DataRow linha in Model.Tables[0].Rows)
                                    {
                                <tr>
                                    <th>
                                            @linha["nome"].ToString()
                                    </th>

                                    <td>
                                       <!--<button href=@Url.Action("Questao", new { idMateria = linha["id"].ToString() }) type="button" class="btn btn-info btn-resolver" data-questao="@linha["questao"].ToString()" data-respostaA="@linha["respostaA"].ToString()" data-respostaB="@linha["respostaB"].ToString()" data-respostaC="@linha["respostaC"].ToString()">Resolver</button>-->
                                       <button type="button" class="btn btn-info btn-resolver" data-questao="@linha["questao"].ToString()" data-respostaa="@linha["respostaA"].ToString()" data-respostab="@linha["respostaB"].ToString()" data-respostaC="@linha["respostaC"].ToString()">Resolver</button>
                                    </td>
                                </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div> 
            <!-- /.col -->
        </div>
        <!-- /.row -->
    </section>
    <!-- /.content -->
</div>